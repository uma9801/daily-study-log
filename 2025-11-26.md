## .str.containsで複数条件を指定する
```python
df[df['職業'].str.contains('社会人|社長')]
```
## DataFrame参照時の条件式
```python
df.loc[(df['身長'] >= 20) & (df['身長'] <= 25), ['身長', '体重'])
```
- pandasでは条件式は1つずつ`&`で繋ぐ。`20 <= df['身長'] <= 25`とは書けない。

## DataFrame名.メソッド名 or pd.関数名
❓: pd.cutとDataFrame名.meanを例にあげて質問です。なぜ、pdに続くメソッドとDataFrame名に続くメソッドに分かれているのですか？全てDataFrame名.メソッド名と統一して、DataFrame名.cutのようにした方が分かり易いと思うのですが。

🤖: ご質問ありがとうございます。Pandasにおける`DataFrame名.メソッド`と`pd.関数`の違いについてですね。これはPandasの設計思想に関わる重要なポイントです。

**`DataFrame名.メソッド` (例: `df.mean()`)**

- これらは通常、特定のDataFrameオブジェクトに直接関連する操作です。つまり、そのDataFrameのデータに対して計算や変換を行います。
- `mean()`、`sum()`、`sort_values()`、`dropna()`などは、DataFrameのインスタンスが持つ「データそのもの」に対する操作であり、そのDataFrameの内部状態を利用したり、その結果を返したりします。

**`pd.関数` (例: `pd.cut()`)**

- これらはPandasライブラリ全体に属する、より汎用的な関数です。特定のDataFrameインスタンスに紐づくものではなく、Pandasが提供するユーティリティ機能として位置づけられています。
- `pd.cut()`は、任意のSeriesや配列のようなデータを「ビン」（区間）に分割するために設計されています。DataFrameの特定の列（Series）を引数として受け取りますが、DataFrameオブジェクト自体がこの関数を「所有」しているわけではありません。他にも`pd.concat()`（複数のDataFrameを結合）、`pd.to_datetime()`（データ型変換）なども`pd.関数`の例です。

**なぜ分かれているのか？**

- **役割の区別:** 特定のDataFrameのデータを直接操作する「メソッド」と、データ構造（DataFrameやSeries）を引数として受け取り、一般的なデータ処理を行う「関数」を区別するためです。
- **再利用性と汎用性:** `pd.cut()`のような関数は、DataFrameの列だけでなく、他のNumPy配列やSeriesオブジェクトなど、さまざまなデータに対して適用できるため、`pd`名前空間に置くことでより汎用的なツールとして提供されています。
- **コードの明確性:** `df.cut()`のようにしてしまうと、`cut`が`df`という特定のDataFrameに特化した機能であるかのように誤解される可能性があります。`pd.cut(df['身長'], ...)`とすることで、「`pandas`ライブラリの`cut`関数を使って、`df`の`身長`列を処理している」という意図がより明確になります。

このように、Pandasは機能の役割や適用範囲に応じて、メソッドとグローバル関数を使い分けていると考えることができます。

❓: 特定のDataFrame、つまりオブジェクトのデータに処理の焦点が合っているのがメソッドでありDataFrame名.メソッド名となる。参照・置換・追加などデータ内容を直接扱う点が特徴。特定の対象に依らず、データ構造という枠組みに焦点が合っているのが（グローバル）関数でありpd.関数名となる。.cut, .concat, .merge,など、特定の値ではなく汎用的な構造を扱う点が特徴。以上の解釈で間違い、不足捕捉があれば教えて。

🤖: はい、その解釈で非常によく本質を捉えられています。ほとんど間違いはありませんし、的確なポイントを挙げられていると思います。いくつか補足するとしたら、以下の点でしょうか。

**`DataFrame名.メソッド名`について：**

- 「特定のDataFrameオブジェクトのデータに処理の焦点が合っている」という点はまさにその通りです。これらのメソッドは、そのDataFrameが**所有するデータ**に対する操作であり、DataFrameの内部状態を利用したり、そのDataFrame自体を（またはそのコピーを）変更したり、そのDataFrameから情報を抽出したりします。
- 「参照・置換・追加などデータ内容を直接扱う点が特徴」というのも適切です。

**`pd.関数名`について：**

- 「特定の対象に依らず、データ構造という枠組みに焦点が合っている」という理解も非常に良いです。これらの関数は、DataFrameやSeriesといった**データ構造を引数として受け取り**、それらに対して汎用的な操作を行います。結果として新しいDataFrameやSeriesを返すことが多いです。
- `pd.concat()`や`pd.merge()`のように、複数のDataFrameを組み合わせて新しいDataFrameを作成する場合など、特定の1つのDataFrameに属するメソッドとしては不自然な操作によく使われます。
- `pd.cut()`や`pd.to_datetime()`のように、あるデータ型から別のデータ型への変換や、連続値をカテゴリに分割するような、より**汎用的な変換・加工ツール**として提供されていると考えると良いでしょう。

**まとめると：**

- **メソッド (`df.method()`)**: 「このDataFrameに何をさせるか？」という、**DataFrameオブジェクト自身に紐づくアクション**です。
- **関数 (`pd.function()`)**: 「これらのデータ構造を使って、Pandasが提供するどんな処理を適用するか？」という、**Pandasライブラリ全体が提供するユーティリティ**です。
## ブール値インデックス
条件式でブール値のSeries型を作り、そのブール値データをインデックス（参照するか否か）に利用する。
## ブロードキャスト演算
行列数の違う配列同士で演算を行う際に、自動的に行列数を合わせて処理する機能。